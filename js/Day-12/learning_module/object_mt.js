// Creating an object
let person = {
  name: "John",
  age: 30,
};

// Mutating the object by changing a property value
person.age = 31;

// Adding a new property
person.gender = "Male";

// Removing a property
delete person.age;

console.log(person);